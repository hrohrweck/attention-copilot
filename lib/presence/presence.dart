// placeholder

/// Marker for the presence layer: screen-lock and idle detection per OS
/// (dbus on Linux, win32+ffi on Windows, native on macOS, fake for tests).
/// Real content lands in todo 14.
class PresencePlaceholder {
  const PresencePlaceholder();
}
