// placeholder

/// Marker for the domain layer: pure Dart models and policy logic
/// (agenda, alert policy, alert engine). Real content lands in todos 4-6.
class DomainPlaceholder {
  const DomainPlaceholder();
}
